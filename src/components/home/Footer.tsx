export function Footer(){

    return (
        <section
        className={`flex justify-center items-center w-full bg-green-100 dark:bg-gray-900 dark:text-gray-200 h-10`}>
            <p className={`font-thin text-gray-700 `}>&copy;2023 todos direitos reservados by sidebit.dev</p>
        </section>
    )
}